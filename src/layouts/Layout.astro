---
import Nav from "@/components/Nav.astro";
import "@/styles/global.css";

const { title = "Daniel — Frontend Dev" } = Astro.props;
---
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
  </head>
  <body>
    <a
      href="#contenido"
      class="sr-only focus:not-sr-only focus:fixed focus:top-3 focus:left-3 focus:z-[60] focus:rounded-md focus:bg-white/90 focus:px-3 focus:py-2 focus:text-sm"
    >
      Saltar al contenido
    </a>

    <Nav />

    <main id="contenido" class="mx-auto max-w-4xl px-6">
      <slot />
    </main>
  </body>
</html>
